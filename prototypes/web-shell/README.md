# Web Shell Prototype

This directory contains the previous React / Vite Thought Atlas UI prototype.

It is intentionally outside the main app path. Thought Atlas Core does not treat this prototype as production responsibility, and root-level validation/build scripts do not depend on it.

Run it only when reviewing old UI interaction ideas:

```bash
npm install
npm run dev
```
